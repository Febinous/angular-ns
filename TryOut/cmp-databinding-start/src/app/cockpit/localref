#<name>-- reference to the element
    --on function arument 
        -- nameInpute:HTMLInputElement
            --nameInput.value
    --using @ViewChild('Localvar')var:ElementRef
    -- Localvar.nativeElement.value